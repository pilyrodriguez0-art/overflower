paper.insrall(window);
window.addEventListener('load', function () {
  paper.setup('flowerCanvas');
  const circle = new Path.Circle(
    view.center,
    50
    );
  cicle.fillColor = 'yellow';
});

                        
